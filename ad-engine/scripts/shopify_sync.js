/**
 * shopify_sync.js — INTENTIONALLY DISABLED in CMO-Agent.
 *
 * This script belongs to the original shopify-ad-engine (at
 * C:\Users\User\APPS\shopify-ad-engine). It pulls products from Shopify.
 *
 * CMO-Agent's ad-engine is a multi-brand rendering pipeline — not Shopify-
 * specific — so the Shopify sync is not wired up here. If you need to sync
 * Shopify products, use the original repo.
 *
 * This file exists only to preserve the full clone structure. Running it
 * here is a no-op.
 */

console.error("shopify_sync is disabled in CMO-Agent. See OWNERSHIP.md.");
process.exit(2);
