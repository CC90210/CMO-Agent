# Maven Pulse State

`cmo_pulse.json` is runtime-local marketing state. It is written by
`scripts/pulse_publish.py` and read by Bravo/Atlas on the local machine.

Do not commit live pulse JSON. The public contract lives in
`brain/AGENT_ORCHESTRATION.md`.
