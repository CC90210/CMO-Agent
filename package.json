{
  "name": "cmo-agent",
  "version": "1.3.0",
  "description": "Maven — AI Chief Marketing Officer. Multi-brand marketing pipeline with send-safety chokepoint and cross-agent Telegram bridge.",
  "private": true,
  "scripts": {
    "setup": "python scripts/setup.py",
    "telegram": "node telegram_agent.js",
    "test": "python scripts/test_send_gateway.py && python scripts/test_late_publisher.py && python scripts/test_instagram_engine.py && python scripts/test_content_pipeline.py && python scripts/test_performance_reporter.py && python scripts/test_notify.py",
    "audit": "python scripts/self_audit.py",
    "health": "echo 'Run /health in your IDE agent'"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {}
}
